"""Layer 1 — ADK-native evaluation for the card_benefits_finder agent.

Holds the ADK criteria configs (``test_config.json``, ``eval_config.json``), the
custom metric (:mod:`evals.adk.metrics`), the pytest entry point, and the
``adk eval`` CLI runner. See ``README.md`` in this directory.
"""
